module Sepulcas.Android.Native.Certs(
    additionalCertificates
  ) where

import System.X509.Android

import qualified Data.ByteString.Char8 as BS

additionalCertificates :: [SignedCertificate]
additionalCertificates = [

  ]
